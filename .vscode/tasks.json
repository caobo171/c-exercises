{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build-linklist",
            "type": "shell",
            "command": "g++ -g link_list/*",
            "problemMatcher": "$gcc",
            "group": {
                "kind": "build",
                "isDefault": true
            }

        },

        {
            "label": "build-linklist",
            "type": "shell",
            "command": "g++ -g link_list/*",
            "problemMatcher": "$gcc",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}